<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bit.movie.review.dao.MovieDAO">
	<select id="selectAllMovie" resultType="bit.movie.review.dto.Movie">
		SELECT *
		FROM MOVIE
	</select>
	
	<insert id="insertMovie" parameterType="bit.movie.review.dto.Movie">
		INSERT INTO MOVIE(CODENAVER, CODEDAUM, MOVIENAME, POSTERIMGSRC, MOVIEAGE, STARRATING, AUDIENCE, TICKETING, DATE)
		VALUES(#{codeNaver}, #{codeDaum}, #{movieName}, #{posterImgSrc}, #{movieAge}, #{starRating}, #{audience}, #{ticketing}, #{date})
	</insert>
	
</mapper>