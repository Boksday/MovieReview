<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bit.movie.review.dao.ReviewDAO">
	<select id="selectAllReview" resultType="bit.movie.review.dto.Review">
		SELECT id, code, writer, contents, created, starRating
		FROM review
	</select>
	<select id="selectOneReview" resultType="bit.movie.review.dto.Review">
		SELECT id, code, writer, contents, created, starRating
		FROM review
		WHERE code = #{code}
	</select>

	<select id="selectOneReviewById" resultType="bit.movie.review.dto.Review">
		SELECT id, code, writer, contents, created, starRating
		FROM review
		WHERE id = #{id}
	</select>	
	<insert id="insertReview" parameterType="bit.movie.review.dto.Review">
		INSERT INTO review(id, code, writer, contents, created, starRating)
		VALUES(#{id}, #{code}, #{writer}, #{contents}, #{created}, #{starRating})
	</insert>
	
</mapper>